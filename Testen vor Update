-- Auto Lift Script mit UI für Muscle Legends

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local backpack = character:WaitForChild("Backpack")
local trainingDelay = 2  -- Zeit in Sekunden zwischen den Wiederholungen

-- Erstelle das GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = player.PlayerGui
screenGui.Name = "AutoLiftGUI"

local frame = Instance.new("Frame")
frame.Parent = screenGui
frame.Size = UDim2.new(0, 200, 0, 100)
frame.Position = UDim2.new(0, 10, 0, 10)
frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
frame.BackgroundTransparency = 0.5

local titleLabel = Instance.new("TextLabel")
titleLabel.Parent = frame
titleLabel.Size = UDim2.new(1, 0, 0, 30)
titleLabel.Position = UDim2.new(0, 0, 0, 0)
titleLabel.Text = "Auto Lift"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextSize = 18
titleLabel.TextAlignment = Enum.TextAlignment.Center

local startButton = Instance.new("TextButton")
startButton.Parent = frame
startButton.Size = UDim2.new(1, 0, 0, 40)
startButton.Position = UDim2.new(0, 0, 0, 40)
startButton.Text = "Start Auto Lift"
startButton.TextColor3 = Color3.fromRGB(255, 255, 255)
startButton.BackgroundColor3 = Color3.fromRGB(0, 128, 0)

local stopButton = Instance.new("TextButton")
stopButton.Parent = frame
stopButton.Size = UDim2.new(1, 0, 0, 40)
stopButton.Position = UDim2.new(0, 0, 0, 80)
stopButton.Text = "Stop Auto Lift"
stopButton.TextColor3 = Color3.fromRGB(255, 255, 255)
stopButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)

local autoLiftActive = false

-- Funktion zum Finden der Hantel
local function findBarbell()
    for _, obj in pairs(workspace:GetChildren()) do
        if obj.Name == "Barbell" and obj:IsA("Model") then
            return obj
        end
    end
    return nil
end

-- Funktion, um die Hantel zu greifen und zu heben
local function autoLift()
    local barbell = findBarbell()
    if barbell then
        -- Setze den Spieler an die Hantel
        character:SetPrimaryPartCFrame(barbell.PrimaryPart.CFrame)
        
        -- Greift die Hantel
        local handle = barbell:FindFirstChild("Handle")
        if handle then
            humanoid:EquipTool(handle)
            wait(0.5)  -- Warte, damit die Hantel korrekt gegriffen wird

            -- Hantel heben (simuliert durch eine einfache Position)
            humanoid:MoveTo(barbell.Position)
            wait(trainingDelay)  -- Warte, bevor der Vorgang wiederholt wird

            -- Hantel ablegen
            humanoid:UnequipTools()
        end
    end
end

-- Start-Button: Auto Lift aktivieren
startButton.MouseButton1Click:Connect(function()
    autoLiftActive = true
    startButton.Text = "Auto Lift Aktiviert"
    startButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)  -- Grün wenn aktiv
    stopButton.Visible = true  -- Stop-Button sichtbar machen
    while autoLiftActive do
        autoLift()
        wait(trainingDelay)
    end
end)

-- Stop-Button: Auto Lift deaktivieren
stopButton.MouseButton1Click:Connect(function()
    autoLiftActive = false
    startButton.Text = "Start Auto Lift"
    startButton.BackgroundColor3 = Color3.fromRGB(0, 128, 0)  -- Zurück zu grün
    stopButton.Visible = false  -- Stop-Button unsichtbar machen
end)

-- Stop-Button ist anfangs unsichtbar
stopButton.Visible = false
