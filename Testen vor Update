--// Services & Player
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

local function getCharacter()
    return player.Character or player.CharacterAdded:Wait()
end

local function getHRP()
    local char = getCharacter()
    return char:WaitForChild("HumanoidRootPart")
end

--// UI erstellen
local gui = Instance.new("ScreenGui")
gui.Name = "MuscleLegendsUI"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0, 220, 0, 280)
frame.Position = UDim2.new(0, 50, 0, 100)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
frame.Active = true
frame.Draggable = true

--// AutoKill UI
local autoKillBtn = Instance.new("TextButton", frame)
autoKillBtn.Size = UDim2.new(0, 200, 0, 40)
autoKillBtn.Position = UDim2.new(0, 10, 0, 10)
autoKillBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
autoKillBtn.TextColor3 = Color3.new(1, 1, 1)
autoKillBtn.Text = "AutoKill: OFF"

--// KillAura UI
local auraBtn = Instance.new("TextButton", frame)
auraBtn.Size = UDim2.new(0, 200, 0, 40)
auraBtn.Position = UDim2.new(0, 10, 0, 60)
auraBtn.BackgroundColor3 = Color3.fromRGB(255, 100, 100)
auraBtn.TextColor3 = Color3.new(1, 1, 1)
auraBtn.Text = "KillAura: OFF"

local auraRangeBox = Instance.new("TextBox", frame)
auraRangeBox.Size = UDim2.new(0, 200, 0, 30)
auraRangeBox.Position = UDim2.new(0, 10, 0, 105)
auraRangeBox.PlaceholderText = "Aura Range (z. B. 25)"
auraRangeBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
auraRangeBox.TextColor3 = Color3.new(1, 1, 1)
auraRangeBox.Text = ""

--// Fly Button
local flyBtn = Instance.new("TextButton", frame)
flyBtn.Size = UDim2.new(0, 200, 0, 40)
flyBtn.Position = UDim2.new(0, 10, 0, 145)
flyBtn.BackgroundColor3 = Color3.fromRGB(50, 150, 255)
flyBtn.TextColor3 = Color3.new(1, 1, 1)
flyBtn.Text = "Fly: OFF"

--// Fly Speed Box
local speedBox = Instance.new("TextBox", frame)
speedBox.Size = UDim2.new(0, 200, 0, 30)
speedBox.Position = UDim2.new(0, 10, 0, 190)
speedBox.PlaceholderText = "Fly-Speed (z. B. 50)"
speedBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
speedBox.TextColor3 = Color3.new(1, 1, 1)
speedBox.Text = ""

--// Sichtbarkeits-Toggle
local eyeBtn = Instance.new("TextButton", gui)
eyeBtn.Size = UDim2.new(0, 40, 0, 40)
eyeBtn.Position = UDim2.new(1, -50, 0, 0) -- noch weiter oben
eyeBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
eyeBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
eyeBtn.Text = "👁️"
eyeBtn.Font = Enum.Font.SourceSans
eyeBtn.TextSize = 30

local uiVisible = true
eyeBtn.MouseButton1Click:Connect(function()
    uiVisible = not uiVisible
    frame.Visible = uiVisible
end)

--// Statusvariablen
local autoKill = false
local auraEnabled = false
local auraRange = 25
local speed = 50
local flying = false

--// Fly-BodyMover
local bv = Instance.new("BodyVelocity")
bv.Velocity = Vector3.zero
bv.MaxForce = Vector3.new(9e9, 9e9, 9e9)
bv.Name = "FlyVelocity"

local bg = Instance.new("BodyGyro")
bg.P = 9e4
bg.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
bg.Name = "FlyGyro"

--// KillAura Visual
local auraVisual = Instance.new("Part")
auraVisual.Shape = Enum.PartType.Ball
auraVisual.Anchored = true
auraVisual.CanCollide = false
auraVisual.Transparency = 0.6
auraVisual.Color = Color3.fromRGB(255, 0, 0)
auraVisual.Material = Enum.Material.ForceField
auraVisual.Size = Vector3.new(auraRange * 2, auraRange * 2, auraRange * 2)
auraVisual.Visible = false
auraVisual.Parent = workspace

--// Button Logik
autoKillBtn.MouseButton1Click:Connect(function()
    autoKill = not autoKill
    autoKillBtn.Text = "AutoKill: " .. (autoKill and "ON" or "OFF")
end)

auraBtn.MouseButton1Click:Connect(function()
    auraEnabled = not auraEnabled
    auraBtn.Text = "KillAura: " .. (auraEnabled and "ON" or "OFF")
    auraVisual.Visible = auraEnabled
end)

auraRangeBox.FocusLost:Connect(function()
    local val = tonumber(auraRangeBox.Text)
    if val and val > 0 then
        auraRange = val
        auraVisual.Size = Vector3.new(auraRange * 2, auraRange * 2, auraRange * 2)
    end
end)

flyBtn.MouseButton1Click:Connect(function()
    flying = not flying
    flyBtn.Text = "Fly: " .. (flying and "ON" or "OFF")
    flyBtn.BackgroundColor3 = flying and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(200, 0, 0)
end)

speedBox.FocusLost:Connect(function()
    local s = tonumber(speedBox.Text)
    if s and s > 0 then
        speed = s
    end
end)

--// Aktionen
task.spawn(function()
    while true do
        if autoKill then
            local char = getCharacter()
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp then
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                        local enemyHRP = p.Character.HumanoidRootPart
                        enemyHRP.CFrame = hrp.CFrame * CFrame.new(0, 0, -3)
                        for _, part in pairs(char:GetChildren()) do
                            if part:IsA("Tool") or part:IsA("Part") then
                                pcall(function()
                                    firetouchinterest(part, enemyHRP, 0)
                                    firetouchinterest(part, enemyHRP, 1)
                                end)
                            end
                        end
                    end
                end
            end
        end
        task.wait(0.5)
    end
end)

RunService.Heartbeat:Connect(function()
    local char = getCharacter()
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    -- KillAura
    auraVisual.Position = hrp.Position
    if auraEnabled then
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                local enemyHRP = p.Character.HumanoidRootPart
                if (enemyHRP.Position - hrp.Position).Magnitude <= auraRange then
                    enemyHRP.CFrame = hrp.CFrame * CFrame.new(0, 3, -3)
                    ReplicatedStorage.rEvents.hitRemote:FireServer(p.Character)
                end
            end
        end
    end

    -- Fly
    if flying then
        bv.Parent = hrp
        bg.Parent = hrp
        local cam = workspace.CurrentCamera
        bg.CFrame = cam.CFrame
        local moveVec = char:FindFirstChild("Humanoid") and char.Humanoid.MoveDirection or Vector3.zero
        if moveVec.Magnitude > 0 then
            local direction = cam.CFrame:VectorToWorldSpace(moveVec).Unit
            bv.Velocity = direction * speed
        else
            bv.Velocity = Vector3.zero
        end
    else
        bv.Parent = nil
        bg.Parent = nil
    end
end)

--// UI persistieren nach Respawn
player.CharacterAdded:Connect(function()
    task.wait(1)
    local newChar = player.Character
    if flying then
        local newHRP = newChar:WaitForChild("HumanoidRootPart")
        bv.Parent = newHRP
        bg.Parent = newHRP
    end
end)
