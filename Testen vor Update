-- AutoHit (Clientseitig, Delta-kompatibel)
-- Aktiviert dauerhaft dein Tool, falls du eins ausgerüstet hast

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function Attack()
    local character = LocalPlayer.Character
    if character then
        local tool = character:FindFirstChildOfClass("Tool")
        if tool then
            tool:Activate()
        end
    end
end

-- Dauerschleife mit task.wait() für bessere Performance
while task.wait(0.1) do
    Attack()
end
