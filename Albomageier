--// UI erstellen
local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local AutoKillButton = Instance.new("TextButton")
local FlyButton = Instance.new("TextButton")

local player = game.Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hrp = char:WaitForChild("HumanoidRootPart")
local humanoid = char:WaitForChild("Humanoid")

ScreenGui.Parent = player:WaitForChild("PlayerGui")
ScreenGui.Name = "MuscleLegendsUI"

Frame.Size = UDim2.new(0, 220, 0, 160)
Frame.Position = UDim2.new(0, 50, 0, 100)
Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Frame.Parent = ScreenGui

-- AutoKill Button
AutoKillButton.Size = UDim2.new(0, 200, 0, 50)
AutoKillButton.Position = UDim2.new(0, 10, 0, 15)
AutoKillButton.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
AutoKillButton.TextColor3 = Color3.fromRGB(255, 255, 255)
AutoKillButton.Text = "AutoKill: OFF"
AutoKillButton.Parent = Frame

-- Fly Button
FlyButton.Size = UDim2.new(0, 200, 0, 50)
FlyButton.Position = UDim2.new(0, 10, 0, 85)
FlyButton.BackgroundColor3 = Color3.fromRGB(50, 
